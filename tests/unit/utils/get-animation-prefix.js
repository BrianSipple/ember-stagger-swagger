import getAnimationPrefix from 'dummy/utils/get-animation-prefix';
import { module, test } from 'qunit';

module('Unit | Utility | set animation event listener');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getAnimationPrefix();
  assert.ok(result);
});
