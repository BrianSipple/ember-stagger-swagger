import setElementStyleProp from 'dummy/utils/set-element-style-prop';
import { module, test } from 'qunit';

module('Unit | Utility | set element style prop');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = setElementStyleProp();
  assert.ok(result);
});
